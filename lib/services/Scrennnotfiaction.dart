import 'package:flutter/material.dart';

class NotifavationScreen extends StatelessWidget {
  const NotifavationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
